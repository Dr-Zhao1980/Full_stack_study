
############################################################
# .gitignore 说明（本文件用于控制哪些文件/目录不进入 Git 提交）
#
# 目标：只提交“核心代码/必要配置/说明文档”，避免把以下内容提交到仓库：
# - 依赖安装目录（例如 node_modules）
# - 构建产物与编译输出（例如 dist、target、*.class）
# - 缓存与临时文件（例如 __pycache__、Vite 缓存、各类 *.tmp）
# - IDE/编辑器生成的配置目录（例如 .vscode、.idea）
# - 日志文件与可能包含敏感信息的环境变量文件（.env，当前已被你注释关闭）
#
# 提醒：
# - .gitignore 只能阻止“尚未被 Git 跟踪”的文件进入提交。
#   如果某些文件以前已经 commit/track 过，需要先取消跟踪（git rm --cached ...）。
############################################################

# ===== OS / Editor（操作系统与编辑器常见垃圾文件） =====
# macOS Finder 产生的目录索引文件
.DS_Store
# Windows 资源管理器产生的缩略图缓存
Thumbs.db
# Vim/部分编辑器产生的交换文件
*.swp
*.swo
# IDE（集成开发环境/编辑器的本机配置）
# VS Code 的工作区设置（通常是个人环境相关，不建议提交）
.vscode/
# JetBrains 系列 IDE（IntelliJ IDEA / PyCharm 等）的工程配置（通常是个人环境相关）
.idea/
# IntelliJ 的模块文件
*.iml

# ===== Logs（运行日志） =====
# 运行时日志往往体积增长快，且可能包含敏感信息，不建议提交
*.log
logs/

# ===== Secrets / Env（环境变量/密钥文件） =====
# 你当前把这一段注释掉了：意味着 .env 文件“不会被忽略”，可能会被提交。
# 一般强烈建议忽略 .env，并提交一个 .env.example（只放示例键，不放真实密钥）。
# 如果你确实希望把 .env 也纳入版本管理，请确保其中不包含任何密钥。
# .env
# .env.*
# !.env.example

# ===== Node / Frontend（前端：Vite/Node 相关） =====
# node_modules 是依赖安装目录：体积大、平台相关、可由 lockfile 还原，必须忽略
node_modules/

# npm/yarn/pnpm 的调试日志：通常仅用于本地排查问题
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 前端构建产物目录：由源码构建生成，不建议提交（除非你有“发布静态站点源码仓库”的特殊需求）
dist/
dist-ssr/

# Vite 相关缓存目录：用于加速本地开发/构建
.vite/
.vite-cache/
.cache/

# ===== Java / Maven（后端 Java：Maven/Spring 等常见） =====
# Maven 构建输出目录：包含编译产物/临时文件
target/
# Java 编译产物：class 文件属于构建结果，不提交
*.class

# ===== Python（后端 Python：解释器缓存/虚拟环境等） =====
# Python 字节码缓存目录：由解释器自动生成
__pycache__/
# Python 字节码文件
*.py[cod]

# Python 虚拟环境目录：包含本地依赖，体积大，且应由 requirements.txt 重建
.venv/
venv/
env/
alice.json
zzn16.json
# pip 的一些构建/元数据目录（某些场景会出现）
pip-wheel-metadata/

# 常见测试/类型检查/格式化工具缓存（即便你暂未使用，保留也不影响）
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ===== Common build artifacts（通用临时文件/备份文件） =====
# 各类工具/脚本可能产生的临时文件
*.tmp
*.temp
*.bak
